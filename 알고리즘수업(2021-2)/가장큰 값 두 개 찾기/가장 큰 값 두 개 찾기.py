def two_max(A, left, right):
    if left == right:
        return A[left], None
    
    mid=(left+right)//2
    ####################

    M=0#M의 초기값
    m=0#m의 초기값
    for i in range(left,mid+1): #L1과 L2 구하기
        return
        











    
    '''L1, L2 비교
    if left != mid: #L1, L2 존재
        if A[left]>=A[mid]:
            M=left
        else:
            M=mid
    if left ==mid: #L2가 None일 때
        M=left #L1 반환
    
    #R1, R2 비교
    if mid+1 !=len(A)-1: #R1, R2 존재
        if A[mid+1]>=A[right]:
            m=mid+1
        else:
            m=right
    if mid+1 ==len(A)-1: #R2가 None일 때
        m=mid+1 #R1 반환'''
    
        

        